---
- name: todolab
  hosts: todolab
  become: yes
  vars_files:
    - vars/config.yml
  roles:
    - debops.apt
    - tersmitten.locales
    - tersmitten.timezone
    - pablocrivella.nvm
    - ocha.yarn
    - geerlingguy.git
    - { role: kosssi.gitconfig, become: no }
    - DavidWittman.redis
    - jdauphant.nginx
